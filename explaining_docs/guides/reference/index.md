# Reference

Reference guides are **information-oriented**. They contain technical descriptions of the machinery and how to operate it (e.g., API boundaries, configurations, lists of things).

*Currently empty. Future references to be added:*
- *Environment Variables (`.env`) full reference*
- *REST API Endpoint documentation*
- *List of built-in Agents and their activation triggers*
- *Macro Variable interpolation list (`{{char}}`, `{{user}}`, etc.)*
